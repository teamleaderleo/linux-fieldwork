name: systemd vmspawn guest bind proof

on:
  push:
    branches:
      - research/foundational-systems-deep-dive-2026-08-03
    paths:
      - .github/workflows/systemd-vmspawn-guest-bind-proof.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  vmspawn-guest-bind:
    runs-on: ubuntu-24.04
    timeout-minutes: 150

    steps:
      - name: Check out fieldwork carrier
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Check out exact systemd repair branch
        uses: actions/checkout@v4
        with:
          repository: teamleaderleo/systemd
          ref: repair/vmspawn-unmapped-bind-userns-2dd0706
          path: systemd
          fetch-depth: 1
          persist-credentials: false

      - uses: systemd/mkosi@f7762b71437227922a367bb89597843c77494ef9

      - name: Free disk space
        run: |
          sudo mv /usr/local /usr/local.trash
          sudo mv /opt/hostedtoolcache /opt/hostedtoolcache.trash
          sudo systemd-run rm -rf /usr/local.trash /opt/hostedtoolcache.trash

      - name: Prepare workspace
        working-directory: systemd
        run: |
          set -euxo pipefail
          truncate --size=100G btrfs.raw
          mkfs.btrfs btrfs.raw
          sudo mkdir -p /mnt/mkosi
          LOOP="$(sudo losetup --find --show --direct-io=on btrfs.raw)"
          rm -f btrfs.raw
          sudo mount "$LOOP" /mnt/mkosi --options compress=zstd:1,user_subvol_rm_allowed,noatime,discard=async,space_cache=v2
          sudo chown "$(id -u):$(id -g)" /mnt/mkosi
          mkdir -p /mnt/mkosi/tmp
          ln -sf /mnt/mkosi/build build

      - name: Configure Arch test image
        working-directory: systemd
        run: |
          set -euxo pipefail
          cat >mkosi/mkosi.local.conf <<'EOF'
          [Distribution]
          Distribution=arch
          Release=rolling

          [Output]
          Format=disk

          [Build]
          UseSubvolumes=yes
          ToolsTreeDistribution=arch
          ToolsTreeRelease=rolling
          WithTests=yes
          WorkspaceDirectory=/mnt/mkosi/tmp
          PackageCacheDirectory=/mnt/mkosi/tmp/cache
          Environment=
                  CFLAGS="-Og"
                  MESON_OPTIONS=--werror
                  LLVM=0
                  SYSEXT=1

          [Runtime]
          RAM=4G
          EOF

      - name: Build current test image
        working-directory: systemd
        run: |
          set -euxo pipefail
          mkosi --debug genkey
          sudo mkosi -f box -- true
          sudo mkosi box -- \
            meson setup \
            --buildtype=debugoptimized \
            -Dbpf-framework=disabled \
            build
          sudo mkosi box -- meson compile -C build mkosi

      - name: Run TEST-87 only
        working-directory: systemd
        run: |
          set -euxo pipefail
          sudo mkosi box -- \
            env \
            TEST_PREFER_QEMU=1 \
            TEST_NO_QEMU=0 \
            TEST_NO_KVM=0 \
            TEST_RUNNER=linux-fieldwork \
            meson test \
            -C build \
            --timeout-multiplier=2 \
            --no-rebuild \
            --setup=integration \
            --suite=integration-tests \
            --print-errorlogs \
            --no-stdsplit \
            --num-processes=1 \
            TEST-87-AUX-UTILS-VM

      - name: Upload focused receipt
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: systemd-vmspawn-guest-bind-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            systemd/build/meson-logs/testlog.txt
            systemd/build/test/journal/*.journal
          if-no-files-found: warn
          retention-days: 7

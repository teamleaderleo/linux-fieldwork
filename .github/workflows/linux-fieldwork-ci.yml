name: Linux Fieldwork CI

on:
  pull_request:
    paths:
      - "tools/**"
      - "tests/**"
      - "scripts/**"
      - "investigations/**"
      - "notes/**"
      - "programmes/**"
      - "targets/**"
      - "upstream/**"
      - ".github/workflows/linux-fieldwork-ci.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lab-tools:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Check out proposed repository state
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Validate changed patch carriers
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          patch_list="$(mktemp)"
          trap 'rm -f "$patch_list"' EXIT
          git diff --name-only --diff-filter=ACMRT -z \
            "${{ github.event.pull_request.base.sha }}" \
            "${{ github.event.pull_request.head.sha }}" \
            -- ':(glob)**/*.patch' >"$patch_list"
          mapfile -d '' patches <"$patch_list"
          if ((${#patches[@]})); then
            python3 tools/validate_unified_diffs.py -- "${patches[@]}"
          else
            echo "No added or modified patch carriers."
          fi

      - name: Compile Python tools
        shell: bash
        run: |
          targets=()
          [[ -d tools ]] && targets+=(tools)
          [[ -d tests ]] && targets+=(tests)
          if ((${#targets[@]})); then
            python3 -m compileall -q "${targets[@]}"
          fi

      - name: Run unit tests
        shell: bash
        run: |
          if [[ -d tests ]] && find tests -type f -name 'test*.py' -print -quit | grep -q .; then
            python3 -m tools.run_fieldwork_unittests --verbosity 2
          fi

      - name: Check shell syntax and command help
        shell: bash
        run: |
          for script in \
            scripts/capture-linux-context.sh \
            scripts/preflight-mmdebstrap-investigation.sh \
            scripts/reproduce-mmdebstrap-autopkgtest.sh; do
            if [[ -f "$script" ]]; then
              bash -n "$script"
            fi
          done
          for tool in \
            tools/tar_manifest.py \
            tools/manifest_diff.py \
            tools/debian_bug_report.py \
            tools/validate_unified_diffs.py \
            tools/run_fieldwork_unittests.py; do
            if [[ -f "$tool" ]]; then
              python3 "$tool" --help >/dev/null
            fi
          done
          if [[ -f scripts/capture-linux-context.sh ]]; then
            bash scripts/capture-linux-context.sh --help >/dev/null
          fi

  capture-bug-report:
    if: >-
      (github.event_name == 'workflow_dispatch' && startsWith(github.ref_name, 'investigation/mmdebstrap-autopkgtest')) ||
      (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository && startsWith(github.head_ref, 'investigation/mmdebstrap-autopkgtest'))
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Check out proposed repository state
        uses: actions/checkout@v4

      - name: Capture Debian bug 1141078
        run: |
          python3 tools/debian_bug_report.py 1141078 \
            --output-dir investigations/mmdebstrap-autopkgtest-1141078/runs/bts-1141078

      - name: Upload BTS capture
        uses: actions/upload-artifact@v4
        with:
          name: debian-bug-1141078
          path: investigations/mmdebstrap-autopkgtest-1141078/runs/bts-1141078/
          if-no-files-found: error
          retention-days: 14

  reproduce-mmdebstrap:
    if: >-
      (github.event_name == 'workflow_dispatch' && startsWith(github.ref_name, 'investigation/mmdebstrap-autopkgtest')) ||
      (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository && startsWith(github.head_ref, 'investigation/mmdebstrap-autopkgtest'))
    runs-on: ubuntu-24.04
    timeout-minutes: 180
    env:
      RUN_ID: gha-${{ github.run_id }}-${{ github.run_attempt }}
      FIELDWORK_EVENT_NAME: ${{ github.event_name }}
      FIELDWORK_EVENT_SHA: ${{ github.sha }}
      FIELDWORK_PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
      FIELDWORK_PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
      FIELDWORK_REF: ${{ github.ref }}
      FIELDWORK_HEAD_REF: ${{ github.head_ref }}
      FIELDWORK_BASE_REF: ${{ github.base_ref }}
      FIELDWORK_RUN_ID: ${{ github.run_id }}
      FIELDWORK_RUN_ATTEMPT: ${{ github.run_attempt }}
      FIELDWORK_EXPECTED_CHECKOUT_CLASSIFICATION: synthetic-merge-ref
    steps:
      - name: Check out proposed repository state
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Run Debian sid autopkgtest in a disposable container
        shell: bash
        run: |
          set -uo pipefail
          run_dir="investigations/mmdebstrap-autopkgtest-1141078/runs/$RUN_ID"
          mkdir -p "$run_dir"
          checkout_rev_line=$(git rev-list --parents -n 1 HEAD)
          set +e
          docker run --privileged --rm \
            --volume "$PWD:/work" \
            --workdir /work \
            --env RUN_ID="$RUN_ID" \
            --env AUTOPKGTEST_TIMEOUT=160m \
            --env FIELDWORK_CHECKOUT_REV_LINE="$checkout_rev_line" \
            --env FIELDWORK_EVENT_NAME \
            --env FIELDWORK_EVENT_SHA \
            --env FIELDWORK_PR_HEAD_SHA \
            --env FIELDWORK_PR_BASE_SHA \
            --env FIELDWORK_REF \
            --env FIELDWORK_HEAD_REF \
            --env FIELDWORK_BASE_REF \
            --env FIELDWORK_RUN_ID \
            --env FIELDWORK_RUN_ATTEMPT \
            --env FIELDWORK_EXPECTED_CHECKOUT_CLASSIFICATION \
            debian:sid-slim \
            bash -lc '
              set -euo pipefail
              export DEBIAN_FRONTEND=noninteractive
              apt-get update
              apt-get install -y --no-install-recommends \
                autopkgtest ca-certificates patch python3 procps util-linux
              apt-get clean
              bash scripts/reproduce-mmdebstrap-autopkgtest.sh
            '
          status=$?
          set -e
          printf '%s\n' "$status" >"$run_dir/container-exit-status"
          exit 0

      - name: Upload reproduction evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mmdebstrap-reproduction-${{ env.RUN_ID }}
          path: investigations/mmdebstrap-autopkgtest-1141078/runs/${{ env.RUN_ID }}/
          if-no-files-found: warn
          retention-days: 14

      - name: Classify reproduction result
        if: always()
        shell: bash
        run: |
          status=$(cat "investigations/mmdebstrap-autopkgtest-1141078/runs/$RUN_ID/container-exit-status")
          case "$status" in
            0)
              echo "The contained autopkgtest passed."
              ;;
            77)
              echo "The contained autopkgtest returned a neutral result."
              ;;
            *)
              echo "The contained autopkgtest failed with status $status."
              exit "$status"
              ;;
          esac

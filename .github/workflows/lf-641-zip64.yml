name: lf-641 retired broad validator

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Superseded by parser-local LF-641 validation.'

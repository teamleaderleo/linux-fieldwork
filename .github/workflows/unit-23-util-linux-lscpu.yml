name: Unit 23 util-linux lscpu cpuset

on:
  pull_request:
    paths:
      - "upstream-packets/units/23-util-linux-lscpu-cpuset/**"
      - ".github/workflows/unit-23-util-linux-lscpu.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  transition-hold:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "unit 23 workflow held while the stable-update composition gate is installed"

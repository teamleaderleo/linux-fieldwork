name: Retired mmdebstrap patch application

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Explain status
        run: echo 'The candidate patch is already applied. Run Verify mmdebstrap explicit TMPDIR handling instead.'

# Handoff

Start with `README.md` for the observed contract and implementation map. `observations.md` preserves the wider GNU tar probe. `PLAN.md` lists the execution sequence. `STATUS.md` holds the latest workflow/review state.

The candidate is intentionally stacked:

1. apply `../tarfilter-transform-target-scopes/tarfilter-transform-target-scopes.patch`;
2. apply `tarfilter-transform-occurrence-selectors.patch`;
3. run `tests/test_tarfilter_transform_occurrences.py`.

The next unresolved grammar slices remain `x`, `flags=` and semicolon-separated statements, BRE translation, and case-conversion escapes under #36.

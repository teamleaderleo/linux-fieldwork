# Scope

Included: decimal occurrence selectors, zero, `g` interaction, `i` composition, repeated decimal runs, and independent target-field counters.

Excluded: extended-regex `x`, persistent `flags=` statements, semicolon-separated expressions, complete BRE translation, case-conversion replacement escapes, upstream submission, and changes to the imported source.

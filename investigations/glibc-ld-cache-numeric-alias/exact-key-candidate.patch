diff --git a/elf/dl-cache.c b/elf/dl-cache.c
index c6820a9f41..5d1a2e3b4c 100644
--- a/elf/dl-cache.c
+++ b/elf/dl-cache.c
@@ -276,6 +276,14 @@ search_cache (const char *string_table, uint32_t string_table_size,
 			  != 0)))
 		break;
 
+	      /* _dl_cache_libcmp provides the cache sort order, but distinct
+		 decimal spellings can compare equal.  Keep ordering equivalence
+		 separate from cache-key identity.  */
+	      if (strcmp (name, string_table + lib->key) != 0)
+		continue;
+
 	      flags = lib->flags;
 	      if (_dl_cache_check_flags (flags)
 		  && _dl_cache_verify_ptr (lib->value, string_table_size))

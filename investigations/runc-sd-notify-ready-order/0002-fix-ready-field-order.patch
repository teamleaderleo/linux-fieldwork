diff --git a/notify_socket.go b/notify_socket.go
--- a/notify_socket.go
+++ b/notify_socket.go
@@ -131,5 +131,5 @@ func (s *notifySocket) run(pid1 int) error {
 			for line := range bytes.SplitSeq(got, []byte{'\n'}) {
-				if bytes.HasPrefix(got, []byte("READY=")) {
+				if bytes.HasPrefix(line, []byte("READY=")) {
 					fileChan <- line
 					return
 				}

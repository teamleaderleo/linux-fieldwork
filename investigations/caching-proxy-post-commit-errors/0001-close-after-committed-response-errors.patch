diff --git a/upstream/mmdebstrap/caching_proxy.py b/upstream/mmdebstrap/caching_proxy.py
--- a/upstream/mmdebstrap/caching_proxy.py
+++ b/upstream/mmdebstrap/caching_proxy.py
@@ -104,3 +104,4 @@ class ProxyRequestHandler(http.server.BaseHTTPRequestHandler):
 
         # download fresh copy
+        response_started = False
         try:
@@ -110,2 +111,3 @@ class ProxyRequestHandler(http.server.BaseHTTPRequestHandler):
             assert (res.status, res.reason) == (200, "OK"), (res.status, res.reason)
+            response_started = True
             self.wfile.write(b"HTTP/1.1 200 OK\r\n")
@@ -132,2 +134,6 @@ class ProxyRequestHandler(http.server.BaseHTTPRequestHandler):
         except Exception as e:
-            self.send_error(502)
+            print(f"proxy error: {e!r}", file=sys.stderr)
+            if response_started:
+                self.close_connection = True
+                return
+            self.send_error(502)

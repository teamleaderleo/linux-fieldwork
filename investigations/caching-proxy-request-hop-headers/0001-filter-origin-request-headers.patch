diff --git a/upstream/mmdebstrap/caching_proxy.py b/upstream/mmdebstrap/caching_proxy.py
--- a/upstream/mmdebstrap/caching_proxy.py
+++ b/upstream/mmdebstrap/caching_proxy.py
@@ -14,6 +14,42 @@ oldcachedir = None
 newcachedir = None
 readonly = False
 
+HOP_BY_HOP_REQUEST_HEADERS = {
+    "connection",
+    "keep-alive",
+    "proxy-authenticate",
+    "proxy-authorization",
+    "proxy-connection",
+    "te",
+    "trailer",
+    "transfer-encoding",
+    "upgrade",
+}
+
+
+def origin_request_headers(headers):
+    host_values = headers.get_all("Host", [])
+    if len(host_values) != 1:
+        raise ValueError("proxy request must contain exactly one Host header")
+
+    connection_tokens = set()
+    for value in headers.get_all("Connection", []):
+        connection_tokens.update(
+            token.strip().lower()
+            for token in value.split(",")
+            if token.strip()
+        )
+    if "host" in connection_tokens:
+        raise ValueError("Host cannot be a connection-specific field")
+    blocked = HOP_BY_HOP_REQUEST_HEADERS | connection_tokens
+    result = [
+        (name, value)
+        for name, value in headers.raw_items()
+        if name.lower() not in blocked
+    ]
+    result.append(("Connection", "close"))
+    return result
+
 
 class ProxyRequestHandler(http.server.BaseHTTPRequestHandler):
     def do_GET(self):
@@ -25,5 +61,10 @@ class ProxyRequestHandler(http.server.BaseHTTPRequestHandler):
         sanitizedpath = urllib.parse.unquote(self.path.removeprefix(pathprefix))
         oldpath = oldcachedir / sanitizedpath
         newpath = newcachedir / sanitizedpath
+        try:
+            origin_headers = origin_request_headers(self.headers)
+        except ValueError as error:
+            self.send_error(400, str(error))
+            return
 
         if not readonly:
@@ -76,3 +117,8 @@ class ProxyRequestHandler(http.server.BaseHTTPRequestHandler):
             print(f"\rproxy download: {self.path}", file=sys.stderr)
             conn = http.client.HTTPConnection(self.headers["Host"], timeout=5)
-            conn.request("GET", self.path, None, dict(self.headers))
+            conn.putrequest(
+                "GET", self.path, skip_host=True, skip_accept_encoding=True
+            )
+            for name, value in origin_headers:
+                conn.putheader(name, value)
+            conn.endheaders()
             res = conn.getresponse()

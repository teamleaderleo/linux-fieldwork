--- a/src/overlay.rs
+++ b/src/overlay.rs
@@
     fn mknod(
         &self,
         req: &Request,
         parent: INodeNo,
         name: &OsStr,
         mode: u32,
-        _umask: u32,
+        umask: u32,
         rdev: u32,
         reply: ReplyEntry,
     ) {
+        let mode = mode & !umask;
@@
-        if let Err(e) = crate::sys::fs::mknodat(inner.workdir_fd, &c_wd, mode | 0o755, rdev as u64)
+        let backing_mode = if self.config.xattr_permissions != 0 {
+            mode | 0o755
+        } else {
+            mode
+        };
+        if let Err(e) = crate::sys::fs::mknodat(
+            inner.workdir_fd,
+            &c_wd,
+            backing_mode,
+            rdev as u64,
+        )
         {
@@
     fn mkdir(
         &self,
         req: &Request,
         parent: INodeNo,
         name: &OsStr,
         mode: u32,
-        _umask: u32,
+        umask: u32,
         reply: ReplyEntry,
     ) {
+        let mode = mode & !umask;
@@
     fn create(
         &self,
         req: &Request,
         parent: INodeNo,
         name: &OsStr,
         mode: u32,
-        _umask: u32,
+        umask: u32,
         flags: i32,
         reply: ReplyCreate,
     ) {
+        let mode = mode & !umask;

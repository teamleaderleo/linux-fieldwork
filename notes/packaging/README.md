# Packaging Notes

Use this directory for package formats, package managers, repositories, dependency resolution, source and binary packages, build systems, and release workflows.

Record the distribution, release, repository state, architecture, package version, and relevant configuration whenever they influence the behavior.

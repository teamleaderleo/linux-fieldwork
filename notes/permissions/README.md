# Permission Notes

Use this directory for users, groups, ownership, mode bits, ACLs, capabilities, privilege changes, access checks, and security boundaries.

Record the effective user and group IDs, capabilities, mount options, namespaces, and mandatory access controls whenever they influence the behavior.

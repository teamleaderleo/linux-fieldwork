# Filesystem Notes

Use this directory for paths, mounts, files, directories, links, storage, filesystem semantics, and tools that inspect or modify them.

Record the filesystem type, mount options, privilege level, and cross-mount behavior whenever they affect the result.

# Shell Notes

Use this directory for shell semantics, quoting, expansion, pipelines, redirection, scripts, command composition, and differences between shells.

Record the shell and version whenever behavior depends on them. Prefer examples that expose exact arguments, exit statuses, file descriptors, or expansion results.

diff --git a/upstream/mmdebstrap/tarfilter b/upstream/mmdebstrap/tarfilter
--- a/upstream/mmdebstrap/tarfilter
+++ b/upstream/mmdebstrap/tarfilter
@@ -346,11 +346,29 @@ def main():
     with tarfile.open(fileobj=sys.stdin.buffer, mode="r|*") as in_tar, tarfile.open(
         fileobj=sys.stdout.buffer, mode="w|", format=tarfile.PAX_FORMAT
     ) as out_tar:
+        hardlink_prefix = re.compile(r"^(?:(?:\.\.?/)|/)+")
+        type_excluded_members = set()
+        retained_member_names = set()
+        hardlink_dependency_failure = False
         for member in in_tar:
             if path_filter_should_skip(member):
                 continue
             if type_filter_should_skip(member):
+                normalized_name = hardlink_prefix.sub("", member.name)
+                if normalized_name not in retained_member_names:
+                    type_excluded_members.add(normalized_name)
                 continue
+            if member.islnk() and hardlink_prefix.sub(
+                "", member.linkname
+            ) in type_excluded_members:
+                print(
+                    "hard-link target excluded by type filter: %s -> %s"
+                    % (member.name, member.linkname),
+                    file=sys.stderr,
+                )
+                hardlink_dependency_failure = True
+                break
+            normalized_name = hardlink_prefix.sub("", member.name)
             if args.strip_components:
                 comps = member.name.split("/")
                 # just as with GNU tar, archive members with less or equal
@@ -405,6 +424,8 @@ def main():
                     and member.linkname != original_linkname
                 ):
                     member.pax_headers.pop("linkpath", None)
+            type_excluded_members.discard(normalized_name)
+            retained_member_names.add(normalized_name)
             if member.isfile():
                 with in_tar.extractfile(member) as file:
                     out_tar.addfile(member, file)
@@ -412,6 +433,9 @@ def main():
             else:
                 out_tar.addfile(member)
 
+    if hardlink_dependency_failure:
+        exit(1)
+
 
 if __name__ == "__main__":
     main()

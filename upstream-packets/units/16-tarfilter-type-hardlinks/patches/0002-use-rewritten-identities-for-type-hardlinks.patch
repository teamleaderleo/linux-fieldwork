diff --git a/upstream/mmdebstrap/tarfilter b/upstream/mmdebstrap/tarfilter
--- a/upstream/mmdebstrap/tarfilter
+++ b/upstream/mmdebstrap/tarfilter
@@ -369,50 +369,71 @@ def main():
                 return True
         return False
 
+    hardlink_prefix = re.compile(r"^(?:(?:\.\.?/)|/)+")
+
+    def normalize_hardlink_name(name):
+        return hardlink_prefix.sub("", name)
+
+    def rewrite_name(name, scope, strip_components):
+        if strip_components and args.strip_components:
+            comps = name.split("/")
+            if len(comps) <= args.strip_components:
+                return None
+            name = "/".join(comps[args.strip_components :])
+        if hasattr(args, "trans"):
+            for regex, replacement, occurrence, global_after, scopes in args.trans:
+                if scopes[scope]:
+                    name = _sed_substitute(
+                        regex, replacement, name, occurrence, global_after
+                    )
+        return name
+
     # starting with Python 3.8, the default format became PAX_FORMAT but we
     # are still explicit here in case of future changes.
     with tarfile.open(fileobj=sys.stdin.buffer, mode="r|*") as in_tar, tarfile.open(
         fileobj=sys.stdout.buffer, mode="w|", format=tarfile.PAX_FORMAT
     ) as out_tar:
-        hardlink_prefix = re.compile(r"^(?:(?:\.\.?/)|/)+")
         type_excluded_members = set()
         retained_member_names = set()
         hardlink_dependency_failure = False
         for member in in_tar:
             if path_filter_should_skip(member):
                 continue
+
+            input_member_name = member.name
+            input_linkname = member.linkname
             if type_filter_should_skip(member):
-                normalized_name = hardlink_prefix.sub("", member.name)
-                if normalized_name not in retained_member_names:
-                    type_excluded_members.add(normalized_name)
+                projected_name = rewrite_name(member.name, "r", True)
+                if projected_name is not None:
+                    normalized_name = normalize_hardlink_name(projected_name)
+                    if normalized_name not in retained_member_names:
+                        type_excluded_members.add(normalized_name)
                 continue
-            if member.islnk() and hardlink_prefix.sub(
-                "", member.linkname
-            ) in type_excluded_members:
-                print(
-                    "hard-link target excluded by type filter: %s -> %s"
-                    % (member.name, member.linkname),
-                    file=sys.stderr,
+
+            rewritten_name = rewrite_name(member.name, "r", True)
+            if rewritten_name is None:
+                continue
+            member.name = rewritten_name
+            if member.name != input_member_name:
+                member.pax_headers.pop("path", None)
+
+            if member.islnk():
+                rewritten_linkname = rewrite_name(
+                    member.linkname, "h", True
                 )
-                hardlink_dependency_failure = True
-                break
-            normalized_name = hardlink_prefix.sub("", member.name)
-            if args.strip_components:
-                comps = member.name.split("/")
-                # just as with GNU tar, archive members with less or equal
-                # number of components are not passed through at all
-                if len(comps) <= args.strip_components:
+                if rewritten_linkname is None:
                     continue
-                member.name = "/".join(comps[args.strip_components :])
-                member.pax_headers.pop("path", None)
-                if member.islnk():
-                    link_comps = member.linkname.split("/")
-                    if len(link_comps) <= args.strip_components:
-                        continue
-                    member.linkname = "/".join(
-                        link_comps[args.strip_components :]
-                    )
-                    member.pax_headers.pop("linkpath", None)
+                member.linkname = rewritten_linkname
+            elif member.issym():
+                member.linkname = rewrite_name(
+                    member.linkname, "s", False
+                )
+            if (
+                (member.islnk() or member.issym())
+                and member.linkname != input_linkname
+            ):
+                member.pax_headers.pop("linkpath", None)
+
             member.pax_headers = {
                 k: v
                 for k, v in member.pax_headers.items()
@@ -427,49 +448,26 @@ def main():
                     exit(1)
                 member.uid += args.idshift
                 member.gid += args.idshift
-            if hasattr(args, "trans"):
-                original_name = member.name
-                original_linkname = member.linkname
-                for (
-                    regex,
-                    replacement,
-                    occurrence,
-                    global_after,
-                    scopes,
-                ) in args.trans:
-                    if scopes["r"]:
-                        member.name = _sed_substitute(
-                            regex,
-                            replacement,
-                            member.name,
-                            occurrence,
-                            global_after,
-                        )
-                    if member.islnk() and scopes["h"]:
-                        member.linkname = _sed_substitute(
-                            regex,
-                            replacement,
-                            member.linkname,
-                            occurrence,
-                            global_after,
-                        )
-                    elif member.issym() and scopes["s"]:
-                        member.linkname = _sed_substitute(
-                            regex,
-                            replacement,
-                            member.linkname,
-                            occurrence,
-                            global_after,
-                        )
-                if member.name != original_name:
-                    member.pax_headers.pop("path", None)
+
+            if member.islnk():
+                normalized_linkname = normalize_hardlink_name(
+                    member.linkname
+                )
                 if (
-                    (member.islnk() or member.issym())
-                    and member.linkname != original_linkname
+                    normalized_linkname not in retained_member_names
+                    and normalized_linkname in type_excluded_members
                 ):
-                    member.pax_headers.pop("linkpath", None)
-            type_excluded_members.discard(normalized_name)
-            retained_member_names.add(normalized_name)
+                    print(
+                        "hard-link target excluded by type filter: %s -> %s"
+                        % (input_member_name, input_linkname),
+                        file=sys.stderr,
+                    )
+                    hardlink_dependency_failure = True
+                    break
+
+            normalized_name = normalize_hardlink_name(member.name)
+            type_excluded_members.discard(normalized_name)
+            retained_member_names.add(normalized_name)
             if member.isfile():
                 with in_tar.extractfile(member) as file:
                     out_tar.addfile(member, file)

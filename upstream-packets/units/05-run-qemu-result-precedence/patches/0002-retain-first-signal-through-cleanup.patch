diff --git a/upstream/mmdebstrap/run_qemu.sh b/upstream/mmdebstrap/run_qemu.sh
--- a/upstream/mmdebstrap/run_qemu.sh
+++ b/upstream/mmdebstrap/run_qemu.sh
@@ -45,13 +45,15 @@
 
 cleanup_exit() {
   rv=$?
-  trap - INT TERM EXIT
+  trap '' INT TERM
+  trap - EXIT
   finish "$rv"
 }
 
 cleanup_signal() {
   rv=$1
-  trap - INT TERM EXIT
+  trap '' INT TERM
+  trap - EXIT
   finish "$rv"
 }
 
